import 'package:flutter/material.dart';

class AdminConfiguracoes extends StatelessWidget {
  const AdminConfiguracoes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Tela de Configurações'));
  }
}
